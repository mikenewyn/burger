<h1>Time to devour some Burgers!</h1>
<link rel="icon" href="../public/assets/img/burger1.gif">
<div class="row">
    <!-- Left side (ready to eat) -->
    <div class="col-md-6">
        <h4 class="red_font">Ready to eat!</h4>
        <div id="ready_to_eat">

            <!-- Dynamically Insert Ready Burgers Here -->
            {{#each burgers}}
            {{#unless devoured}}
                <div class="input-group">
                    <li class="form-control">{{id}}. {{burger_name}}</li>
                    <span class="input-group-btn">
                        <button class="btn btn-success devour-button" data-id="{{id}}" type="submit">Devour It!</button>
                    </span>
                </div>
            {{/unless}}
            {{/each}}

        </div>
    </div>

    <!-- Right side (already eaten) -->
    <div class="col-md-6">
        <h4 class="red_font">Already eaten!</h4>
        <div id="already_eaten">

            <!-- Dynamically Insert Eaten Burgers Here -->
            {{#each burgers}}
            {{#if this.devoured}}
            <div class="input-group">
                <li class="form-control">{{this.id}}. {{this.burger_name}}</li>
            </div>
            {{/if}}
            {{/each}}

        </div>
    </div>
</div>

<h2>Add A Burger Option</h2>

<div class="row">
    <div class="col s12">
        <form class="create-form" action="/burgers" method="POST">
            <input type="text" id="newBurger" name="burger_name" placeholder="Your Burger">
            <button type="submit" class="addBurgerButton">Add new Burger</button>
        </form>
    </div>
</div>